/**
 * The vendored Google/Brotli Authors reference decoder (MIT — see LICENSE
 * beside this file). Types are hand-written because the source is plain JS.
 */
export declare function BrotliDecode(bytes: Int8Array): Uint8Array;
